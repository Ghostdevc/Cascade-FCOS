# Ablation C-uniform — Stage 3, radius NOT tightened (1.5 throughout)
run_name: "abl_C_uniform_s3"
data_dir:  "data/VOCdevkit"
stage:     3
tier:      2
epochs:    6
batch_size: 4
num_workers: 4
lr:           0.00002
weight_decay: 0.0001
lr_milestones: [4, 5]
lr_gamma:      0.1
center_radius: 1.5        # <-- uniform
checkpoint_dir: "logs/checkpoints/abl_C_uniform"
with_centerness_repred: true
num_stages: 3
ablation: "C_uniform_radius"
