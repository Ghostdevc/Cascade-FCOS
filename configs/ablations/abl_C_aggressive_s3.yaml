# Ablation C-aggressive — Stage 3, further tightened (0.5)
run_name: "abl_C_aggressive_s3"
data_dir:  "data/VOCdevkit"
stage:     3
tier:      2
epochs:    6
batch_size: 4
num_workers: 4
lr:           0.00002
weight_decay: 0.0001
lr_milestones: [4, 5]
lr_gamma:      0.1
center_radius: 0.5        # <-- aggressive (default is 0.75)
checkpoint_dir: "logs/checkpoints/abl_C_aggressive"
with_centerness_repred: true
num_stages: 3
ablation: "C_aggressive_radius"
